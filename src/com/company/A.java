package com.company;

import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.Scanner;

public class A {
/*
    SimpleDateFormat sdf1 = new SimpleDateFormat("HH:mm");
    SimpleDateFormat sdf2 = new SimpleDateFormat("dd.MM.yyyy");
    SimpleDateFormat sdf3 = new SimpleDateFormat("dd.MM");

    Scanner scan = new Scanner(System.in);
    Date[] dates = new Date[3];
        System.out.println("Enter time: ");
        for (int i = 0; i < dates.length; i++) {
        String a = scan.nextLine();
        dates[i] = sdf1.parse(a);}
    Date minDate = dates[0];
        for (int i = 0; i < dates.length; i++) {
        if (dates[i].before(minDate)) {
            minDate = dates[i];}
    }
        System.out.println("Минимальная из дат: " + sdf1.format(minDate));

    Employee[] emp = new Employee[3];
        for (int i = 0; i < emp.length; i++){
        System.out.print("Enter name: ");
        String name = scan.next();
        System.out.print("Enter birth date");
        String date = scan.next();
        Employee employee = new Employee(name, sdf2.parse(date));
        emp[i] = employee;
    }
    Date today = new Date();
        for (int x = 0; x < emp.length; x++) {
        if (sdf3.format(emp[x].getBirthDate()).equals(sdf3.format(today))) {
            System.out.println("Сегодня день рождения у: " + sdf3.format(emp[x].getBirthDate()));
        } else {
            continue;
        }
    }*/
}